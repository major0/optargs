name: Build

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

permissions:
  contents: read

jobs:
  detect-changes:
    uses: ./.github/workflows/detect-changes.yml

  lint-optargs:
    name: Lint OptArgs Core
    needs: detect-changes
    if: needs.detect-changes.outputs.optargs-changed == 'true'
    uses: ./.github/workflows/lint.yaml

  lint-goarg:
    name: Lint GoArg
    needs: detect-changes
    if: needs.detect-changes.outputs.goarg-changed == 'true'
    uses: ./.github/workflows/lint.yaml
    with:
      directory: goarg

  lint-pflags:
    name: Lint PFlags
    needs: detect-changes
    if: needs.detect-changes.outputs.pflags-changed == 'true'
    uses: ./.github/workflows/lint.yaml
    with:
      directory: pflags

  build-optargs:
    name: Build OptArgs Core
    needs: detect-changes
    if: needs.detect-changes.outputs.optargs-changed == 'true'
    uses: ./.github/workflows/build-module.yml

  build-goarg:
    name: Build GoArg Module
    needs: [detect-changes, build-optargs]
    if: always() && needs.detect-changes.outputs.goarg-changed == 'true'
    uses: ./.github/workflows/build-module.yml
    with:
      directory: goarg

  build-pflags:
    name: Build PFlags Module
    needs: [detect-changes, build-optargs]
    if: always() && needs.detect-changes.outputs.pflags-changed == 'true'
    uses: ./.github/workflows/build-module.yml
    with:
      directory: pflags

  compat-test:
    name: Compatibility Tests
    runs-on: ubuntu-latest
    needs: [detect-changes, build-goarg, build-pflags]
    if: always() && (needs.detect-changes.outputs.goarg-changed == 'true' || needs.detect-changes.outputs.pflags-changed == 'true')
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version: '1.23'
    - name: goarg compat-test
      if: needs.detect-changes.outputs.goarg-changed == 'true'
      working-directory: goarg
      run: make compat-test
    - name: pflags compat-test
      if: needs.detect-changes.outputs.pflags-changed == 'true'
      working-directory: pflags
      run: make compat-test
    - name: Verify golden files present
      run: .github/scripts/compat-golden-count.sh
