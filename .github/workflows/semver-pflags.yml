name: Semver PFlags

on:
  create:
  push:
    branches:
      - 'pflags/v[0-9]+.[0-9]+'
    paths:
      - 'pflags/**'

jobs:
  semver:
    uses: ./.github/workflows/semver.yml
    permissions:
      contents: write
    with:
      prefix: 'pflags/v'
      ref-pattern: 'refs/heads/pflags/v'

  release:
    needs: semver
    if: needs.semver.outputs.tag != ''
    uses: ./.github/workflows/release.yml
    permissions:
      contents: write
    with:
      tag: ${{ needs.semver.outputs.tag }}
